# General Ledger Mock Data

Provides the canonical payload that the Spec-Kit schema expects.

| File | Notes |
| --- | --- |
| `gl_balances_balanced.csv` | Control totals that agree to every balanced subledger. |
| `gl_balances_variance.csv` | Leaves AP and Revenue short, mirroring the variance files.
| `gl_transactions_oct.csv` | Sample detail (journal lines) for tracing issues.
